apply plugin: 'war'

dependencies {
    compile(
            project(':service')
    )
}

apply plugin: 'jetty'
jettyRun{
    webAppSourceDirectory file("$projectDir/src/main/webapp")
    httpPort 8080
    contextPath project.name
    scanIntervalSeconds 0
    reload "automatic"
}